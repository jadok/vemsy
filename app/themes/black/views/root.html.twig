<html>
  <head>
      <title>{{ title }}</title>
    {% for my_style in styles %}
      <link href={{my_style.file}} ref="stylesheet" type="text/css" >
    {% endfor %}
  </head>
  <body>
    <main>
      {{ md_data }}
    </main>
  </body>
</html>